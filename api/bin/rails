#!/usr/bin/env ruby
# Boots the Rails console / command line for this application.
# Usage: bin/rails <command>   (equivalent to `bundle exec rails <command>`)
APP_PATH = File.expand_path('../config/application', __dir__)
require_relative '../config/boot'
require 'rails/commands'
